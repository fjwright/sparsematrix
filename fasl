% -*-REDUCE-*-

symbolic <<
   global '(filelist);

   in filelist$

   for each file in filelist do <<
      faslout file;
      infile concat2(id2string file, ".red");
      faslend;
   >>;
>>;

end;
