% -*-REDUCE-*-

symbolic <<
   global '(filelist);

   in filelist$

   for each file in filelist do <<
      file := id2string file;
      faslout concat2("fasldir/", file);
      infile concat2(file, ".red");
      faslend;
   >>;
>>;

end;
